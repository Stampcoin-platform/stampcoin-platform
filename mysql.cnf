[mysqld]
# Basic MySQL configuration for local Docker deployment
character-set-server = utf8mb4
collation-server = utf8mb4_unicode_ci
max_connections = 500
max_allowed_packet = 256M
skip-host-cache
skip-name-resolve
